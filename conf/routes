# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.index
GET     /timeline                           controllers.Application.timeBrowse
GET     /keywords                           controllers.Application.keywordBrowse
GET     /people                           controllers.Application.personBrowse
GET     /results                           controllers.Application.resultsBrowse
GET     /item                           controllers.Application.itemShow

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
